import LazyLoad from "vanilla-lazyload";
const lazyLoadInstance = new LazyLoad({
  // callback_loaded: el => {
  //   if (
  //     el.parentElement.previousElementSibling.classList.contains("lazy-loader")
  //   ) {
  //     el.parentElement.previousElementSibling.classList.add("hidden-loader");
  //   }
  // },
});
